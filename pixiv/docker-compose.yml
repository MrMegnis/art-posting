version: '3.8'
services:
  pixiv_vote_bot:
    build: .
    container_name: pixiv_vote_bot
    volumes:
      - ./data:/app/data
      - ./vote_bot_pixiv.py:/app/vote_bot_pixiv.py
    env_file:
      - .env
    environment:
      - TZ=Europe/Moscow
    restart: unless-stopped
